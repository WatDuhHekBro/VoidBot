export function yeet(a: number, b: number) {
	return a + b;
}

// Generic function to split an array into equal groups
export function paginate() {
	//
}

// Inserts variables into a string based on a delimiter
export function parseVars() {
	//
}
